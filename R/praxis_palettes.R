# Possible palettes used in Praxis
#' @export

praxis_palettes <- list(
  `main`         = praxis_cols("blue", "lightblue", "black", "red", "yellow", "grey", "gold"),
  `contrast`     = praxis_cols("blue", "red", "yellow"),
  `cool`         = praxis_cols("blue", "grey", "lightblue"),
  `binary`       = praxis_cols("blue", "grey"),
  `blackgold`    = praxis_cols("black", "gold"),
  `monochrome`   = praxis_cols("black","grey"),
  `seq_blue`     = praxis_cols("blue","lightblue","vlightblue"),
  `seq_red`      = praxis_cols("red", "vlightred"),
  `seq_yellow`   = praxis_cols("yellow", "vlightyellow"),
  `seq_gold`     = praxis_cols("gold", "vlightgold"),
  `seq_black`    = praxis_cols("black","grey", "vlightgrey"),
  `diverging_br` = praxis_cols("blue", "vlightgrey", "red"),
  `diverging_bg` = praxis_cols("blue", "vlightgrey", "gold"),
  `diverging_by` = praxis_cols("blue", "vlightgrey", "yellow")
)
