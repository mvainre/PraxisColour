# Colours of Praxis
praxis_colours <- c(
  `blue`       <- "#00599d",
  `coral`      <- "#ff6651",
  `aqua`       <- "#84dad3",
  `yellow`     <- "#ffd85d",
  `light blue` <- "#8cd9fc",
  `green`      <- "#bae18f",
  `grey`       <- "#f2f2f2")
